# Optional -- lib/supabaseClient.js already defaults to the real Mimi's
# Supabase project + anon key (same publicly-safe key already hardcoded in
# dashboard/index.html and kds/index.html). Only set these to point a build
# at a different Supabase project.
NEXT_PUBLIC_SUPABASE_URL=https://igchqqyassrfpsliyjec.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required in Vercel. This is a real secret -- get the value from the n8n
# credential "ktcaEXqFFkXA1wWT" (X-Mimis-Webhook-Secret) and set it in the
# Vercel project's Environment Variables, never commit it here.
MIMIS_WEBHOOK_SECRET=

# Optional -- defaults to https://automation.teamastrixdev.com
MIMIS_N8N_BASE_URL=

# Optional -- VAPID public key for Web Push. Safe to expose (it's meant to be
# public, embedded in every subscriber's PushSubscription -- same model as the
# anon key above). lib/push.js already defaults to the real generated key.
# The matching PRIVATE key is never used here -- it lives only in
# mimis.app_secrets (key='vapid_private_key'), a service-role-only RLS-locked
# table read server-side by the n8n Push Notification Sender workflow. Leave
# this unset unless you intentionally rotate the keypair -- if you do, the
# matching private key in mimis.app_secrets must be updated to match or every
# existing subscriber needs to resubscribe.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
